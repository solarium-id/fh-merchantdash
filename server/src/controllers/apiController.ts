import type { Request, Response } from "express";

// Get All Controller
export const GetApiController = (_req: Request, res: Response) => {
  res.json({ data: "This is the json data" });
};

// Get One Controller

// Post Controller

// Put & Patch Controller

// Delete Controller
